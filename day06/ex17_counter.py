# Ask the user to choose a number
number = int(input("Choose a number: "))

# Loop from 1 up to the chosen number (inclusive)
for i in range(1, number + 1):

    # Print the current number
    print(i)